I already have a MERN full-stack project with an existing backend. Do not rebuild or replace my backend.

Your task is to implement only the React Native mobile frontend inside the existing folder path named app.

First, inspect the project structure and understand the existing backend API routes, controllers, models, authentication flow, JWT usage, request/response formats, and environment variables.

Then create a realistic, working React Native mobile application frontend inside app that connects to the existing backend API.

Requirements:

1. Use React Native / Expo if suitable for the existing setup.
2. Use functional components and React hooks.
3. Create proper folder structure inside app:
   - src/api
   - src/screens
   - src/components
   - src/navigation
   - src/context
   - src/utils
   - src/styles
4. Configure Axios for backend API integration.
5. Use .env for API base URL.
6. Implement JWT authentication:
   - Login
   - Register if backend supports it
   - Store token securely
   - Attach token to protected API requests
   - Logout
7. Create navigation:
   - Auth stack
   - Main app stack
   - Bottom tab navigation if suitable
8. Build mobile screens based on the existing backend features.
9. Do not use hardcoded data. Fetch all data from backend API.
10. Add form validation and proper error messages.
11. Add loading states and empty states.
12. Add role-based UI if backend has roles such as Admin, Manager, Staff, or Client.
13. Make the UI clean, professional, and mobile-friendly using blue, white, and dark navy colors.
14. Make sure the frontend works with the hosted backend API.
15. Add clear setup instructions in app/README.md.

Important:
- Do not change backend logic unless a small fix is absolutely required.
- If backend route names are different, adapt the frontend to the real routes.
- If any API endpoint is missing, clearly mention it instead of inventing fake data.
- Ensure the app runs without errors using npm install and npm start or Expo start.
- After implementation, explain which files were created or updated and how to run the mobile app.